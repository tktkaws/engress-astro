---
import Button from "./Button.astro";
---

<header class="header">
  <h1 class="logo">
    <a href="/index.html">
      <img src="/images/home/logo@2x.png" />
    </a>
    <!-- ソース出し分け？ -->
    <!-- <img src="/images/home/logo.png" /> -->
  </h1>
  <nav class="gnav">
    <ul>
      <li>
        <a href="/index.html">ホーム</a>
      </li>
      <li>
        <a href="/newsIndex.html">お知らせ</a>
      </li>
      <li>
        <a href="/blogIndex.html">ブログ</a>
      </li>
      <li>
        <a href="/price.html">コース・料金</a>
      </li>
    </ul>
  </nav>
  <address>
    <small>平日08:00〜20:00</small>
    <p class="call">
      <img src="images/home/tell.svg" alt="" />
      <span>0123-456-7890</span>
    </p>
  </address>
  <Button title="資料請求" class="accent" />
  <Button title="お問い合わせ" class="main" />
  <!-- <button class="hamburger">
    <span class="hamburgur--inner"></span>
  </button> -->
  <button
    class="navbtn"
    onClick="document.querySelector('body').classList.toggle('open')"
  >
    <span class="navbtn-bar"></span>
    <span class="sr-only">MENU</span>
  </button>

  <nav class="nav center">
    <ul class="menu">
      <li>
        <a href="/index.html">ホーム</a>
      </li>
      <li>
        <a href="/newsIndex.html">お知らせ</a>
      </li>
      <li>
        <a href="/blogIndex.html">ブログ</a>
      </li>
      <li>
        <a href="/price.html">コース・料金</a>
      </li>
    </ul>
    <Button title="資料請求" class="accent" />
    <Button title="お問い合わせ" class="accent" />
  </nav>
</header>

<style is:global lang="scss">
  header {
    padding-left: 15px;
    padding-right: 10px;
    width: 100%;
    height: 80px;
    font-size: 16px;
    /* display: grid; */
    align-items: center;
    /* grid-template-columns: 163px 1fr 186px 152px 137px; */
    display: flex;
  }
  .logo {
    width: 136px;
  }
  .logo img {
    width: 100%;
  }
  address {
    text-align: right;
    margin-right: 23px;
  }
  .call {
    margin-top: 5px;
    font-weight: bold;
    display: flex;
    align-items: center;
    gap: 5px;
    justify-content: flex-end;
  }
  .call img {
  }
  .call span {
  }
  .gnav ul {
    display: flex;
    justify-content: flex-start;
    list-style: none;
    padding-inline: 0;
    gap: 16px;
    font-weight: bold;
  }
  .nav {
  }
  .nav a {
  }

  /* ハンバーガーボタン */
  .navbtn {
    all: unset;
    outline: revert;
    -webkit-tap-highlight-color: transparent;
    cursor: pointer;
    // width: max(46px, var(--size1));
    aspect-ratio: 1/1;
    display: grid;
    place-items: center;
    // color: var(--primary);
    width: 46px;
    color: $c-main;
  }

  .navbtn-bar,
  .navbtn::before,
  .navbtn::after {
    content: "";
    display: block;
    width: 100%;
    height: 2px;
    background-color: currentColor;
    grid-area: 1 / 1;
    transition: transform 0.5s;
  }
  .navbtn::before {
    transform: translateY(-12px);
  }
  .navbtn::after {
    transform: translateY(12px);
  }

  .open .navbtn-bar {
    transform: scale(0);
  }
  .open .navbtn::before {
    transform: translateY(0) rotate(45deg);
  }
  .open .navbtn::after {
    transform: translateY(0) rotate(-45deg);
  }

  .open .navbtn {
    // color: var(--white);
    color: $c-background;
    // color: $c-main;
    z-index: 200;
  }

  /* スクリーンリーダー */
  .sr-only {
    border: 0;
    clip: rect(0 0 0 0);
    height: 1px;
    margin: -1px;
    overflow: hidden;
    padding: 0;
    position: absolute;
    width: 1px;
  }
  /* ナビゲーションメニュー */
  .nav {
    gap: 44px;
    position: fixed;
    top: 0;
    right: -100%;
    width: 100%;
    height: 100%;
    z-index: 100;
    // background: linear-gradient(0.96deg, #1b224c, #ffffff 182.75%);
    background: $c-main;
    // color: var(--white);
    color: $c-background;
    transition: transform 0.5s;
  }

  .open .nav {
    transform: translate(-100%, 0);
  }

  .open {
    position: fixed;
    overflow: hidden;
    width: 100%;
  }
  .center {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
  }
  @media (min-width: 1000px) {
    .navbtn {
      display: none;
    }
  }
  @media (max-width: 999px) {
    .gnav,
    address,
    button.accent,
    button.main {
      display: none;
    }
  }
</style>
